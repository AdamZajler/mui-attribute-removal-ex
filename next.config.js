/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,

    // Comment / uncomment this option below to see in dev-mode that attributes are disappearing but not from the input
    // compiler: {
    //     reactRemoveProperties: {properties: ['^data-cy']},
    // }
}

module.exports = nextConfig
